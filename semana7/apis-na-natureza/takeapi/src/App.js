import React from 'react';

export default class App extends React.Component{
  state={
    seasons: "",
    
  }

  render(){
    return (
      <div>
        select com as temporadas por numero, 
        ao selecionar aparece as infos da temporada(imagem, o nome das queens e a colocação de cada uma)
        quando clica no nome da queen vai pegar 
      </div>
    );
  };
